[project]
name = "ecommerce-eda"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "faststream[cli,redis]>=0.6.5",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
]
